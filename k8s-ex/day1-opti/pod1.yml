apiVersion: v1
kind: Pod
metadata:
 name: demo-pod
spec:
 containers:
 - name: demo-container-1
   image: nginx
   resources:
     limits:
       memory: "900Mi"
       cpu: "900m"
     requests:
       memory: "700Mi"
       cpu: "600m"